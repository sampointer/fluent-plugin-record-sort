RSpec.describe Fluent::Plugin::Record::Sort do
  it 'has a version number' do
    expect(Fluent::Plugin::Record::Sort::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(true).to eq(true)
  end
end
